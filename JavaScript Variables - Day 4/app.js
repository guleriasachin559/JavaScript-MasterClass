var name = 'Jatin' // ---> This is also a string in the JS
var name = "Sachin" //---> This is also a string in the JS
//console.log() // shortcut for this statement is just type the log and then select the option which having the log to the console.
console.log(name);


var age = 10;
console.log(age); // 

console.log(address); //usage is happening at line no 10

var address; // declaration line no 12
console.log(address);

//Redeclaration with the var
        var address
        var address = "Mumbai"
        var address = "delhi"

        console.log(address);

// Block Scope is not applicable to "var" Keyword

for (var i = 0; i < array.length; i++) {
    console.log(i)
    
}

console.log(i+ "outside the blocke i can access");




hello(); // calling 

function hello(params) { // declaration
    console.log(hello);
    
}

